import 'package:flutter/material.dart';

class RatingsTabPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is the ratings tab page.'),
    );
  }
}
